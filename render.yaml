services:
  - type: web
    name: buildloop-backend
    runtime: node
    rootDir: buildloop-backend
    buildCommand: npm ci --omit=dev
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: false
